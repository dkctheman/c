#include <stdio.h>

void main(){

    char str;
    int myi;
    double myd;
    float myf

    scanf(str);
    scanf(myi);
    scanf(myd);
    scanf(myf);

    printf ("%c,%d,%f,%f is a cool program!",str,myi,myd,myf);
}